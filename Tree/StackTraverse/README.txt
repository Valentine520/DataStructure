The 3 kinds of traverse including preorder、inorder、postorder base on stack.
The postorder is the most complicated for it need a result stack to reserve its cur root and the visit the left and right.
So I use double stack and single stack to implementation of single stack traverse.
